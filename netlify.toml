[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19"

# SPA fallback — React Router client routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
